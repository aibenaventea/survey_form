<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Formulario</title>
</head>
<body>
    <form class="form1" action="/result" method="post">
        Your Name: <input class="userName" name="userName" type="text" placeholder="enter your name" required><br>
        Dojo Location: <select name="userLocation">
            <% for(let i in data.location) { %>
            <option class="userLocation" value="<%=data.location[i]%>"><%=data.location[i]%></option>
            <% } %>
        </select><br>
        Favorite Language: <select name="userFavoriteLanguage">
            <% for(let i in data.language) { %>
                <option class="userFavoriteLanguage" value="<%=data.language[i]%>"><%=data.language[i]%></option>
            <% } %>
        </select><br>
        Comment (optional) <br>
        <input class="userComment" name="userComment" type="text" placeholder="enter your comment""><br>
        <button class= "submit">Submit</button>
    </form>
</body>
</html>